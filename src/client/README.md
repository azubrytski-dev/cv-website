# Client App

This is the Next.js frontend for the CV website.

## Scripts

```bash
npm run dev
npm run build
npm start
npm test -- --watchAll=false
```

## Notes

- The app uses the Next.js App Router.
- Route entry points live in `src/app`.
- Shared portfolio content still comes from local mock data for now.
